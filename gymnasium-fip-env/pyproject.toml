[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["fip_env"]

[project]
name = "fip_env"
version = "0.0.1"
dependencies = [
  "gymnasium",
  "torch>=2.5.1",
  "pygame>=2.1.3",
  "pre-commit",
]
